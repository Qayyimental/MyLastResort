// src/config/app-config.ts

export const appConfig = {
    appName: "Automated Financial Program",
    version: "1.0.0",
    apiBaseUrl: "https://api.example.com",
    enableLogging: true,
    defaultCurrency: "USD",
    supportedLanguages: ["en", "es", "fr"],
    maxTransactionLimit: 10000,
    sessionTimeout: 30, // in minutes
};